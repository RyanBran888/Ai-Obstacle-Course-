# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Godot
.godot/
*.import

# exported game build
/builds/
*.pck

# PyInstaller output
AI/infer/
AI/build/
AI/infer.spec
AI/__pycache__/

# runtime-generated
AI/level.json
AI/result.json
# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# Training artifacts (DQN side).
# Checkpoints and run logs are large and regenerable; keep them out of history.
*.pt
*.pth
*.ckpt
*.h5
checkpoints/
runs/
logs/
wandb/
mlruns/
tensorboard/

# Generated environment inspection artifacts.
# Galleries, SVGs, and room dumps are all reproducible from a seed --
# rebuild with `python3 tools/generate_gallery.py` rather than committing them.
# Scoped to output/ on purpose: hand-authored .svg design assets elsewhere in
# the tree should still be tracked.
Architecture/output/
output/

# macOS.
# This repo lives on a non-HFS volume, so Finder scatters AppleDouble sidecars
# (`._name`) next to every file. They are metadata, not source.
.DS_Store
._*
.Spotlight-V100
.Trashes
.fseventsd/
.TemporaryItems

# Windows
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.claude/

# Training reward plots (regenerate by running QN_train.py)
training_rewards*.png
mvp_*_rewards.png
curriculum_training*.png
curriculum_rooms*.json
curriculum_report*.json
